<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <permission android:label="Receive Xposed Broadcast"
        android:name="dev.ujhhgtg.pandorasbox.permission.RECEIVE_XPOSED_BROADCAST" />

    <uses-permission android:name="dev.ujhhgtg.pandorasbox.permission.RECEIVE_XPOSED_BROADCAST" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AimAssistant"
        android:enableOnBackInvokedCallback="true">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AimAssistant">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".services.OverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse"/>

        <receiver android:name=".receivers.ScreenRotationReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.CONFIGURATION_CHANGED" />
            </intent-filter>
        </receiver>

        <receiver android:name=".receivers.StopServiceReceiver" android:exported="false"/>

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

<!--        <receiver android:name=".receivers.ForegroundAppChangeReceiver"-->
<!--            android:exported="true"-->
<!--            android:permission="dev.ujhhgtg.pandorasbox.permission.RECEIVE_XPOSED_BROADCAST">-->
<!--            <intent-filter>-->
<!--                <action android:name="dev.ujhhgtg.pandorasbox.ACTION_FOREGROUND_APP_CHANGE" />-->
<!--            </intent-filter>-->
<!--        </receiver>-->

        <meta-data
            android:name="modularised"
            android:value="true" />

        <meta-data
            android:name="nonprescription"
            android:value="Aim Assistant's helper Xposed module. Provides foreground application change broadcast." />

        <meta-data
            android:name="interconversion"
            android:value="93" />

        <meta-data
            android:name="xposedsharedprefs"
            android:value="true"/>
    </application>
</manifest>